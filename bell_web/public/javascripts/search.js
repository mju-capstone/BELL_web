// 검색하기 버튼 클릭 이벤트
$(document).ready(function(){
    $(".searchBtn").click(function() {
        window.scrollTo({
            top: 1000,
            left: 0,
            behavior: 'smooth'
          });
    });
});